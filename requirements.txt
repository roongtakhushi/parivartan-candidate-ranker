# Core ranking pipeline has zero external dependencies and runs on Python standard libraries.
# The following packages are required only for the Streamlit web sandbox interface:
streamlit>=1.20.0
pandas>=1.3.0
